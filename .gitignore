# venv / python
.venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
anaconda_projects/

# OS
.DS_Store

# nicht zum Projekt gehoerende Uebungs-Notebooks (lokal behalten, nicht pushen)
7_DataCleaning_exercise_airbnb.ipynb
8_DataVisualization_exercise_airbnb.ipynb
8_DataVisualization_slides.ipynb
Untitled1.ipynb

# runs: alles ignorieren AUSSER den im Notebook genutzten runs
# train-2 = im Notebook benutztes Modell (best.pt), train-5 = gezeigte Plots
runs/**
!runs/
!runs/detect/
!runs/detect/train-2/
!runs/detect/train-2/**
!runs/detect/train-5/
!runs/detect/train-5/**
# last.pt nicht noetig, best.pt reicht
runs/detect/train-2/weights/last.pt
runs/detect/train-5/weights/last.pt
# .DS_Store auch in den genutzten runs raushalten
runs/**/.DS_Store

# YOLO label-cache (auto-generiert)
*.cache

# compiled engine exports
*.engine
